export const TINY_FUN = `
  function plusTwo(n) {
		let newValue = n + 2;
		return newValue;
	}
	
	const summer = (a, b) => a + b;

	const nested = function () {
		const a = 1;
		function b(i) {
			return i + 10;
		}
		return b(a);
	};
`;

export const MORE_FUN = ``;
